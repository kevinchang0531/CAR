# CAR

Local project workspace for CAR.

## Current Status

The repository has been initialized with a conservative project skeleton:

- `AGENTS.md` for Codex collaboration rules
- `.gitignore` for local-only and generated files
- `docs/project-dashboard.md` for progress and setup notes

## Next Step

Define the concrete goal for this project, including expected users, input data, output format, and preferred technology stack if any.

## Run

```powershell
npm run dev
```

Open `http://localhost:4174`.
